{
	"url": "/movements/{{parameters.lawsuitId}}",
	"method": "GET",
	"qs": {
		"origin": "{{parameters.origin}}",
		"limit": "{{parameters.limit}}",
		"offset": "{{parameters.offset}}"
	},
	"response": {
		"output": "{{body}}"
	}
}
