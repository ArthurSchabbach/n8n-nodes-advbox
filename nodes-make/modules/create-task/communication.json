{
	"url": "/posts",
	"method": "POST",
	"body": {
		"from": "{{parameters.from}}",
		"tasks_id": "{{parameters.tasks_id}}",
		"lawsuits_id": "{{parameters.lawsuits_id}}",
		"start_date": "{{parameters.start_date}}",
		"guests": "{{parameters.guests}}",
		"{{...}}": "{{parameters.additionalFields}}"
	},
	"response": {
		"output": "{{body}}"
	}
}
